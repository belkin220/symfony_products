<!DOCTYPE html>
<html>
	<head>
<meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
<title>Resumen PDF</title>
<style>
    body {}
    .container {
        width: 100%;
    }
    .page-number {
        float: right;
        margin-top: -40px;
        position: relative
    }
    div.title {
        font-family: Arial, Helvetica, sans-serif;
        font-size: 18px;
        font-weight: bold;
        margin-bottom: 15px;
        display: flex;
        width: 100%;
        margin-left: 35%;
        margin-top:15px
    }
    .row {
        display: -webkit-box;
        display: -ms-flexbox;
        display: flex;
        -ms-flex-wrap: wrap;
        flex-wrap: wrap;
        margin-right: 15px;
        margin-left: 15px;
        margin-top: 15px;
    }
    .table {
        font-family: Arial, Helvetica, sans-serif;
        border-collapse: collapse;
        width: 100%;
    }
    table td, th {
        border: 1px solid #ddd;
        padding: 8px;
    }

    table tr:nth-child(even) {
        background-color: #f2f2f2;
    }

    table th {
        padding-top: 12px;
        padding-bottom: 12px;
        text-align: center;
        background-color: #0acf97;
        color: white;
    }
    .btn-primary {
    color: #fff;
    background-color: #727cf5;
    border-color: #727cf5;
    text-align: center;
    vertical-align: middle;
    cursor: pointer; 
    border: 1px solid transparent;
    padding: 0.45rem 0.9rem;
    font-size: .9rem;
    font-family: Arial, Helvetica, sans-serif;
    border-radius: 0.15rem;
    text-decoration:none;
   
    }
    img {
    width: 50px; /* You can set the dimensions to whatever you want */
    height: 50px;
    object-fit: cover;
    }
    #footer {
    position: fixed;  
    right: 0;
    bottom: 0;
    width: 25px;
    height:25px;
    border-radius:50%;
    background-color: #0acf97;
    color: white;
    text-align: center;
    font-weight:bold;
    margin-right:15px;
    font-family: Arial, Helvetica, sans-serif;
    padding:1px
    }
    #footer .page:after { content: counter(page, decimal); }
   </style>
</head>
	<body>
		<div class="container">
			<div class="title">
				Listado de productos a
				{{date}}
			</div>
            <div id ="footer">
				<p class="page"  style="margin-top:3px"></p>
			</div>
			<div class="row">
				<table class="table">
					<thead>
                        <th>Imagen</th>
						<th>Producto</th>
						<th>Descripción</th>
						<th>Precio</th>
					</thead>
					<tbody>
						{% for product in data %}
							<tr >
                                <td style="text-align:center; width:10%">
                                <img src="http://symfonypdf.dev.com/uploads/images/{{product.image}}" 
                                     width="50" 
                                     height="50" >
                                </td>
								<td>{{product.name}}</td>
								<td>{{product.description}}</td>
								<td style="text-align:right; width:10%">
                                    {{ product.price|number_format(2, ',', '.') }} € 
                                </td>
							</tr>
						{% endfor %}
					</tbody>
				</table>
                <div style="margin-top:25px">
				<a class=" btn-primary" href="/muestra">Volver</a>
                </div>
			</div>
            <div id="footer">
                <p class="page" style="margin-top:3px"></p>
            </div> 
		</div>
	</body>
</html>
